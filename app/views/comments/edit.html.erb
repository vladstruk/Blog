<h3> Edit comment <h3>

<%= form_for @comment, url: article_comment_path(params[:article_id], params[:id]) do |f| %>
  <%= f.label :content %> </br>
  <%= f.text_area :content %> </br>
  <%= f.submit %>
<% end %>