<h3> All users <h3>
<table>
  <th> Id </th>
  <th> Name </th>
  <% @users.each do |user| %>
  <tr>
    <td> <%= user.id %> </td>
    <td> <%= link_to user.name, user_path(user) %> </td>
  </tr>
  <% end %>
</table>
